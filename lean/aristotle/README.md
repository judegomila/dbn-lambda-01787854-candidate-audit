# Aristotle-autoformalized Lean 4 — the complete lemma layer

Seven Lean 4 projects generated by **Harmonic Aristotle** (arXiv:2510.01346,
via the `aristotlelib` API, 2026-08-20) from this repository's theorem
documents, then locally kernel-verified. Together with the existing
`lean/BarrierAnalyticSkeleton.lean`, `lean/HypothesisDischarge.lean`, and
`lean/DeBruijnNewman_BaseCase.aristotle.lean`, they machine-check the
complete logical chain from the certified interval numerics to
$\Lambda\le893927/5000000$, with only the published theorems
(Polymath 1.2/1.3, Platt--Trudgian) and the Arb-certified numerical outputs
entering as explicit hypotheses.

## Verification record (2026-08-20, local)

- Toolchain `leanprover/lean4:v4.28.0`, mathlib per each project's pinned
  `lake-manifest.json`. All seven: `lake build` → `Build completed
  successfully`.
- Zero `sorry`, zero `axiom` declarations, zero `native_decide` across all
  `RequestProject/*.lean` files.
- `#print axioms` on all 223 theorems: every one depends only on
  `[propext, Classical.choice, Quot.sound]`.
- An independent statement-faithfulness review compared every definition and
  theorem against the source documents; verdicts below.

## Projects

| Directory | Source document | Main results |
|---|---|---|
| `ht_continuity/` | `lean/HYPOTHESIS_DISCHARGE_NOTES.md` §2 | The concrete Polymath15 kernel `Phi` (the real series, indexed $n\ge1$); summability, uniform convergence and continuity on $[0,\infty)$; `aestronglyMeasurable_Phi` and `integrable_exp_mul_norm_Phi` — **exactly** the hypotheses $h_\Phi$/$h_{\rm dom}$ of `lean/HypothesisDischarge.lean`, so hypothesis (a) of the barrier skeleton is now discharged up to the one-line real-to-complex coercion. |
| `tail_lemma/` | `TAIL_LEMMA.md` | Exact mollifier weights and parameters (Lemma 0), endpoint cap, exact convolution identity, contraction bound, all-$N$ reduction, $y$-monotonicity, error monotonicity, and the packaged `tail_theorem` with H1/H2/H3 and the cap/YM gates as explicit hypotheses. |
| `window_freeze/` | `WINDOW_FREEZE_THEOREM.md` | `window_freeze` over the full domain $690988\le N\le3840000$; the site-bracket margins $5377393.9878$ and $11989041.1746$ **proved outright** from mathlib's $\pi$ bounds (stronger than the submitted packaging, which permitted them as hypotheses); exact half-open coverage and pairwise disjointness. |
| `native_binding/` | `NATIVE_BINDING.md` | Concrete $\lambda_d=\prod_{p\mid d}(-b_t(p))$, the Euler-product/Dirichlet-sum identity, both exact convolution identities (including the conjugate), and `native_triangle`: $L_N>0\Rightarrow L_N\le\|f\|$ under Theorem 1.3's decomposition hypotheses, with a satisfiability witness. |
| `error_weld/` | `ERROR_CONSTANT_WELD.md` | The exact identity $179/50+173/25=21/2$; `conservative_weld` deriving the boxed $10.50/(x-12)$ bound from Proposition 6.6(vi) (as hypothesis) uniformly on $x\ge200$. |
| `derivative_box/` | `DERIVATIVE_BOX_LEMMA.md` | The uniform spatial envelope (Polymath Lemma 8.4's pointwise bound as hypothesis), the quantitative $c_x>0$ gate, the $A$-core slope gate, the head-plus-integral majorant, holomorphic-quadrature facts on cut-avoiding balls, and the convex two-disk prism homotopy with the exact $1/800$ radius enlargement. |
| `theorem12_weld/` | `PROOF_NOTE.md` §2, `README.md` | The real `Phi`, $H_t$, $\xi$, and the genuine de Bruijn--Newman characterization; all exact row rationals **proved** (including $t_0+y_0^2/2=893927/5000000$ by `norm_num`); condition (i) derived from Platt--Trudgian via the proved $\eta(\sigma)>0$ grouping argument (only the classical $\eta=(1-2^{1-\sigma})\zeta$ identity enters as hypothesis); condition (iii) derived from the rectangle certificate by containment; the final `weld` concluding $\Lambda\le893927/5000000$. |

## Hypothesis boundary (what is assumed, not proved)

- Polymath Theorem 1.2 (`theorem12_weld`: `T12Hyp`) and Theorem 1.3's
  decomposition and bounds (`tail_lemma`: `hH1`; `native_binding`:
  Hypothesis H).
- Polymath error majorants (71)–(72) / Prop 6.6(vi) (`tail_lemma`: `hH2`;
  `error_weld`: `hP66`) and Lemma 8.4's pointwise bound (`derivative_box`).
- Platt--Trudgian verified height $T_{\rm PT}=3000175332800$
  (`theorem12_weld`: `PTHyp`) and the classical $\eta$–$\zeta$ identity
  (`EtaZetaHyp`).
- The Arb-certified numerical outputs (H3 of the tail lane; the FT and BR
  certificate conclusions in the weld). These correspond one-to-one to this
  repository's sealed interval certificates.

Projects `ht_continuity`, `window_freeze`, and `error_weld`'s Lemmas 1–2 are
unconditional in-file.

## Corrections applied after generation

Two statement-level defects in `tail_lemma/RequestProject/Tail.lean` found by
the faithfulness review were corrected by hand and re-kernel-verified
(2026-08-20): the H3 error constant `11672/10^15` was a transcription of the
source's `11672/10^12` (the file's own docstring already said
$1.1672\cdot10^{-8}$), and hypothesis H2 was over-quantified
($\forall N\ge N_*$, jointly unsatisfiable with `herr3`'s strict positivity)
and is now existential at the window index, as in the source. No other file
was modified from Aristotle's output.

## Autoformalization caveat

Proofs are kernel-verified; faithfulness of statements and definitions to
the intended mathematics was checked by one independent review pass (verdicts
above) but still warrants referee eyes, exactly as for
`DeBruijnNewman_BaseCase.aristotle.lean`.

## Build

Each project directory is self-contained: `lake exe cache get && lake build`
with the pinned toolchain. `ARISTOTLE_SUMMARY.md` in each directory is
Aristotle's own change summary, preserved verbatim.

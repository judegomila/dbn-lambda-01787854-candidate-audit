"""Improved-bound research lane.

Unsealed.  Nothing here is evidence for the candidate, and nothing in the
sealed tree may cite it; see REVIEW_SCOPE.md.  Promotion into
``independent/`` is the deliberate step that changes that, and it happens
only once the criteria in ``core.py`` are met.

The current certified claim is Lambda <= 893927/5000000 = 0.1787854 and it
stays frozen while this lane develops.  A referee is mid-review against
that number; moving it under him is not a thing to do by accident.
"""

from __future__ import annotations

__all__ = ["core", "report", "cli"]
